from setuptools import setup, find_packages

setup(
    name='log',
    version='1.0',
    packages=find_packages(),
    install_requires=[
    ],
    # Metadata
    author='Joowon Jang',
    author_email='jereiard@gmail.com',
    description='Custom logger',
    url='https://github.com/jereiard',
)
