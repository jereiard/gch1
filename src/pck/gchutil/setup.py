from setuptools import setup, find_packages

setup(
    name='gchutil',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        "pandas",
    ],
    # Metadata
    author='Teresa Perinan Tocino',
    author_email='',
    description='GP2 terra platform utils',
    url='',
)
